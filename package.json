{
  "name": "scrumpoker",
  "version": "0.1.0",
  "description": "An agile tool",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd Client && npm install && cd ../Server/ && npm install",
    "bundle": "cd Client && npm run build",
    "start": "node Server/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^1.17.5"
  }
}
